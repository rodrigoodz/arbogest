export enum Pages {
  Home = "/",
  Login = "/login",
  Neighborhood = "/neighborhood",
  Tree = "/tree",
  Register = "/register",
  PasswordReset = "/passwordReset",
}

export enum SearchParam {
  Id = "id",
  StartDate = "startDate",
  EndDate = "endDate",
}
